import {mkdir,cp,writeFile,readdir} from 'node:fs/promises';await mkdir('dist/docs',{recursive:true});await cp('docs','dist/docs',{recursive:true});const files=(await readdir('docs',{recursive:true})).filter(x=>x.endsWith('.md'));await writeFile('dist/docs/index.html','<!doctype html><meta charset=utf-8><title>ZeroSpend Docs</title><h1>ZeroSpend documentation</h1><ul>'+files.map(f=>`<li><a href="${f}">${f}</a></li>`).join('')+'</ul>');console.log(`docs_build=PASS pages=${files.length}`);
