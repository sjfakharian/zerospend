# NVIDIA API Catalog

The hosted OpenAI-compatible base is `https://integrate.api.nvidia.com/v1`. Eligibility requires current official `Free Endpoint` evidence, authenticated catalog presence, and a successful bounded chat probe. Paid deployments are never selected automatically.
