# OmniRoute

OmniRoute is optional. ZeroSpend admits a route only when it adds useful capacity, is actually available, and has independent zero-cost evidence. Duplicate proxy paths do not automatically qualify.
