# macOS installation

Install Node.js 22+ through an official package or Homebrew, clone the repository, then run `./install.sh`. The installer refuses occupied ports and uses only user-owned runtime directories. Configure optional LaunchAgents from the templates after reviewing paths and ports.
