# Linux installation (experimental)

Install Node.js 22+ and npm, run `npm install --ignore-scripts`, then `node cli/zerospend.mjs setup`. Start services under your user account. A maintained systemd installer is not included in v0.1.0.
