# Observability

SQLite stores timestamps, routes, providers, models, token counts, latency, TTFT, status, fallbacks, tool-call outcomes, and free verification. It does not store prompts, completions, SQL, files, tool arguments/results, or MCP payloads. WAL mode and bounded retention keep writes lightweight.
