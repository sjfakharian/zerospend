# ZeroSpend Console

The loopback console shows strict-free health, live metadata routing, models, topology, benchmarks, token and performance analytics, providers, safety evidence, automation, and configuration history. `npm run demo` uses synthetic fixtures and contacts no provider.
