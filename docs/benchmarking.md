# Benchmarking

The weekly runner uses a small synthetic shortlist for code, SQL, reasoning, general, tools, fast responses, long context, and structured output. It separates controlled benchmark quality from production reliability and requires a meaningful promotion margin. Running it consumes provider quota.
