# OpenAI-compatible clients

Use API base `http://127.0.0.1:20129/v1`, model `smart-free`, and the local bearer token. Chat completions, SSE streaming, tools, and structured-output requests follow the OpenAI-compatible request shape supported by the selected provider.
