# Hermes Agent

Install Hermes from its official distribution. Configure a custom OpenAI-compatible provider with base URL `http://127.0.0.1:20129/v1`, model `smart-free`, and the local ZeroSpend bearer token. Verify current Hermes configuration keys against its official documentation because its config format can change.
