import http from 'node:http';import {readFile} from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';import {MetadataStore} from '../../packages/observability/src/store.mjs';import {paths} from '../../packages/shared/src/paths.mjs';
const ROOT=path.dirname(fileURLToPath(import.meta.url)),p=paths(),demo=process.env.ZEROSPEND_DEMO==='1',HOST='127.0.0.1',PORT=Number(process.env.ZEROSPEND_CONSOLE_PORT||20131);const fixture=JSON.parse(await readFile(path.resolve(ROOT,'../../fixtures/demo/dashboard.json'),'utf8'));let store;if(!demo)try{store=new MetadataStore(path.join(p.data,'observability.db'))}catch{}
const send=(r,s,v,t='application/json')=>{const b=t==='application/json'?JSON.stringify(v):v;r.writeHead(s,{'content-type':t,'cache-control':'no-store'});r.end(b)};
http.createServer(async(req,res)=>{try{const u=new URL(req.url||'/',`http://${HOST}:${PORT}`);if(req.socket.remoteAddress!=='127.0.0.1'&&req.socket.remoteAddress!=='::1')return send(res,403,{error:'loopback only'});if(u.pathname==='/health')return send(res,200,{status:'ok',demo,metadata_only:true});if(u.pathname==='/api/dashboard')return send(res,200,demo?fixture:{...fixture,events:store?.recent(100)||[]});const f=u.pathname==='/'?'index.html':u.pathname.slice(1);if(!/^[\w./-]+$/.test(f)||f.includes('..'))return send(res,400,{error:'invalid path'});const ext=path.extname(f),type=ext==='.css'?'text/css':ext==='.js'?'text/javascript':'text/html';send(res,200,await readFile(path.join(ROOT,'public',f)),type)}catch(e){send(res,500,{error:e.message})}}).listen(PORT,HOST,()=>console.log(`ZeroSpend Console http://${HOST}:${PORT}${demo?' (demo)':''}`));
