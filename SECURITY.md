# Security Policy

Do not report API keys or sensitive logs in public issues. Report vulnerabilities privately through GitHub Security Advisories. Supported security fixes target the latest release.

ZeroSpend binds to loopback by default, stores secrets outside the repository with owner-only permissions, and prohibits prompt/response telemetry. Treat changing the bind address as a security-sensitive deployment decision.
