import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp,readFile} from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {fileURLToPath} from 'node:url';import {installAutomation,automationStatus} from '../../packages/automation/src/index.mjs';
test('installs schedules into isolated home',async()=>{const home=await mkdtemp(path.join(os.tmpdir(),'zs-automation-')),repo=path.dirname(path.dirname(path.dirname(fileURLToPath(import.meta.url)))),files=await installAutomation({repoRoot:repo,nodePath:'/usr/bin/node',home});assert.equal(files.length,2);const text=await readFile(files[0],'utf8');assert(text.includes('/usr/bin/node'));assert(!text.includes('__HOME__'));assert.equal(automationStatus({home})[0].schedule,'Daily 04:10')});
