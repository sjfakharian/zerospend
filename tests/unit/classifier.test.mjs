import test from 'node:test';import assert from 'node:assert/strict';import {classify} from '../../packages/router/src/classifier.mjs';
test('tools outrank other signals',()=>assert.equal(classify({messages:[{role:'user',content:'SELECT *'}],tools:[{type:'function'}]}).alias,'free-tools'));
test('classifies SQL',()=>assert.equal(classify({messages:[{role:'user',content:'write a SELECT with GROUP BY'}]}).alias,'free-sql'));
test('classifies code',()=>assert.equal(classify({messages:[{role:'user',content:'debug this stack trace'}]}).alias,'free-code'));
test('falls back to general',()=>assert.equal(classify({messages:[{role:'user',content:'Please consider the background information and provide an appropriate response regarding the situation described, while keeping the answer clear and concise for the intended audience and without assuming any specialist domain. The context includes several observations from different perspectives, historical background, constraints, stakeholder expectations, examples, caveats, and supporting details. Synthesize those materials into a coherent response that preserves nuance and remains accessible to readers who have not seen the source material.'}]}).alias,'free-general'));
