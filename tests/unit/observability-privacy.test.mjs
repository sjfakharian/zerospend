import test from 'node:test';import assert from 'node:assert/strict';import {mkdtemp} from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {MetadataStore} from '../../packages/observability/src/store.mjs';
test('forbidden content cannot be persisted',async()=>{const h=await mkdtemp(path.join(os.tmpdir(),'zs-privacy-')),s=new MetadataStore(path.join(h,'metadata.db'));s.record({timestamp:new Date().toISOString(),request_id:'privacy',client:'test',prompt:'private prompt',completion:'private completion',sql:'SELECT private',tool_arguments:{secret:true},tool_output:'private',credentials:'private',status:200,success:true});const row=s.recent(1)[0];for(const key of ['prompt','completion','sql','tool_arguments','tool_output','credentials'])assert.equal(key in row,false)});
