# Contributing

Open an issue before substantial architecture changes. Keep pull requests focused and include tests. Run `npm run lint && npm run typecheck && npm test && npm run build && npm run docs:build`.

Use synthetic fixtures. Never include credentials, prompts, responses, private logs, or company data. Provider additions must document current zero-cost evidence and failure behavior.
